[workspace]

[package]
name = "downstream-service"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "downstream-service"
path = "main.rs"

[dependencies]
axum = "0.8"
tokio = { version = "1", features = ["rt-multi-thread", "macros"] }
